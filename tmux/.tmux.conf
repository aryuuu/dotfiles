# set -g default-terminal "screen-256color"
set -sg escape-time 0
set -g default-terminal "tmux-256color"
# set -g default-terminal "alacritty"

# this one is for urxvt
# set -sa terminal-overrides ',rxvt-unicode-256color:RGB'

## ALACRITTY COLOR RELATED
# only this line might not be enough
set -g default-terminal "screen-256color"
# Needs this line also to overrides the default color
set-option -ag terminal-overrides ",xterm-256color:RGB"

set -g mouse on
set -ga terminal-overrides ',*256color*:smcup@:rmcup@'

# # Allow scrolling with Page Up and Page Down keys
# bind-key -T root-page-up page-up copy-mode
# bind-key -T root-page-down page-down copy-mode
# bind-key -T copy-mode-vi page-up history-up
# bind-key -T copy-mode-vi page-down history-down

# send prefix
set -g prefix2 M-b
set -g status-left-length 30 # could be any number

# go to nvim
# bind -r G run-shell "~/.local/share/nvim/site/pack/packer/start/harpoon/scripts/tmux/switch-back-to-nvim"
bind -n M-n run-shell "~/.local/share/nvim/site/pack/packer/start/harpoon/scripts/tmux/switch-back-to-nvim"

# reload config
bind -r r source-file ~/.tmux.conf \; display-message "~/.tmux.conf reloaded"

# tmux choose-tree shortcut
bind -n M-g choose-tree

# window navigation
bind -n M-h previous-window
bind -n M-l next-window

# session navigation
bind -n M-r switch-client -l

## powerline like rice
## color scheme (styled as vim-powerline)

set -g status-left-length 52
set -g status-right-length 451
set -g status-fg black
set -g status-bg colour232
# set -g pane-border-fg colour245
# set -g pane-active-border-fg colour39
# set -g message-fg colour16
# set -g message-bg colour221
# set -g message-attr bold
# set -g status-left '#[fg=colour235,bg=colour252,bold] #S #[fg=colour252,bg=colour238,nobold]#[fg=colour245,bg=colour238,bold] #(hostname) #[fg=colour238,bg=colour234,nobold]'

#
#
# set -g status-left '#[fg=colour252,bg=colour235,bold]#[fg=colour235,bg=colour252,bold] #S #[fg=colour252,bg=colour238,nobold]#[fg=colour245,bg=colour238,bold]#[fg=colour238,bg=colour234,nobold]'

# # bubbly look
# set -g status-left '#[fg=colour252,bg=colour235,bold]#[fg=colour235,bg=colour252,bold]#S#[fg=colour252,bg=colour235,bold] '
# set -g window-status-format '#[fg=colour252,bg=colour235,bold]#[fg=colour31,bg=colour252]#I #[fg=colour31,bg=colour252,bold]#W#[fg=colour252,bg=colour235,bold]'
# set -g window-status-current-format '#[fg=colour75,bg=colour235,bold]#[fg=colour235,bg=colour75]#I #[fg=colour235,bold]#W#[fg=colour75,bg=colour234,nobold]#[fg=colour75,bg=colour235,bold]'

# powerline look
set -g status-left '#[fg=colour235,bg=colour252,bold] #S '
set -g window-status-format '#[fg=colour31,bg=colour252] #I #[fg=colour31,bg=colour252,bold] #W '
set -g window-status-current-format '#[fg=colour235,bg=colour75] #I #[fg=colour235,bold] #W '

# set -g window-status-format "#[fg=colour10,bg=colour234] #I #[fg=colour231,bold]#W#F #[fg=colour31,bg=colour234,nobold]"
# set -g window-status-current-format "#[fg=colour10,bg=colour31] #I #[fg=colour231,bold]#W  #[fg=colour31,bg=colour234,nobold]"
# ^ tip, to list colors use this in bash:
#   for i in {0..255}; do printf "\x1b[38;5;${i}mcolour${i} "; done; echo

# List of plugins
# set -g @plugin 'tmux-plugins/tpm'
# set -g @plugin 'tmux-plugins/tmux-sensible'
# set -g @plugin 'catppuccin/tmux'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'github_username/plugin_name#branch'
# set -g @plugin 'git@github.com:user/plugin'
# set -g @plugin 'git@bitbucket.com:user/plugin'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
